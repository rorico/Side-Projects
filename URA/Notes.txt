CameraGUI_OpeningFcn
    mycam = char(caminfo.InstalledAdaptors(2));
    change number based on adaptor corresponding to camera


SelectROI_Callback
    height and width


problems with choosing and deleting boxes on the preview axes.


src.exposureMode = 'Auto' to src.exposureAuto = 'On'
(same with gain)


exposure and gain don't update on the bar, but they do change the camera.


one rotation is about 36nm
width of camera is about 40nm

connecting camera to new computer - download adaptor (outside of matlab)